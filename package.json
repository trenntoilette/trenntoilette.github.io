{
  "name": "Trenntoilette",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "git rm -rf docs/ && nuxt generate && copyfiles .nojekyll docs/ && copyfiles CNAME docs/ && git add docs/* && git commit -am 'deploying' && git push"
  },
  "dependencies": {
    "@nuxtjs/robots": "^2.5.0",
    "@nuxtjs/sitemap": "^2.4.0",
    "copyfiles": "^2.4.1",
    "core-js": "^3.19.3",
    "nuxt": "^2.15.8",
    "nuxt-jsonld": "1.5.3",
    "vue": "^2.6.14",
    "vue-server-renderer": "^2.6.14",
    "vue-template-compiler": "^2.6.14",
    "webpack": "^4.46.0"
  },
  "devDependencies": {
    "@nuxt/image": "^0.7.1"
  }
}
